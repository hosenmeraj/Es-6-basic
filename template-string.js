
//template string and dynamic string
const first = `Hello how are you 
hope you are well
i think you are so happy.`;
const age = 20;
const friends = ['kadu', 'madu', 'sadu', 'jadu'];
const newAge = `i am ${age} years old.we are ${friends.length} member. we need ${friends.length * 1000} taka per member.`;
console.log(newAge);